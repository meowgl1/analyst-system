# v2.0 → v2.1 — 2026-06-07

## Summary

Expanded from 6 agents + 8 scripts to **11 agents + 27 scripts + 8 departments**.
Introduced global orchestrator, 4 new departments, librarian skill router, GitHub repo.

---

## New departments

| Department | Agent | Scripts |
|---|---|---|
| network | `network-analyst` | dns-enum, port-profiler, connection-monitor |
| threat-intel | `threat-hunter` | ioc-checker, mitre-mapper, osint-domain |
| cloud | `cloud-auditor` | dockerfile-audit, env-leak-scanner, vercel-config-audit |
| identity | `identity-auditor` | auth-flow-analyzer, permission-scanner |

## New scripts (forensics + dependency-audit)

Previously these departments had no backend scripts — agents used raw bash commands.

| Department | Scripts added |
|---|---|
| forensics | persistence-scanner, binary-verifier, log-analyzer |
| dependency-audit | package-analyzer, lockfile-auditor |

## New shared utility

`backend/tools/report-builder.py` — aggregates JSON findings from any script into a structured markdown report. Used by all department agents.

## New agents

- `analyst` — global orchestrator with activation matrix (full / external / local / pre-deploy / project)
- `network-analyst`, `threat-hunter`, `cloud-auditor`, `identity-auditor`

## Studio changes

- Librarian installed: `.studio/skills/librarian/` with pre-computed `skills-index.json` (754 skills, 45 subdomains, 452KB)
- Removed 754 symlinks from `.claude/skills/` — context bloat eliminated
- Agents moved from `.claude/agents/` to `.studio/agents/` (source of truth), symlinked back
- `.gitignore` updated: new `security-audits/` subdirs added
- `context.md`, `tasks.md`, `gotchas.md`, `STUDIO.md` updated to v2.1

## GitHub

Repository initialized and pushed: https://github.com/meowgl1/analyst-system
